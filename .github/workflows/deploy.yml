name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment disabled. Using Cloudflare Pages."
